name: Deploy preview
# GitHub Pages for this repo is sourced from the gh-pages branch (legacy),
# not GitHub Actions. Keep this workflow disabled so main pushes do not go red.
on:
  workflow_dispatch:
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pages source is gh-pages. Push that branch to publish."
